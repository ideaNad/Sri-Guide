# Copy to .env.production on the server (or set in your host/CI).
# Public URL the browser uses to reach the API (must match Nginx /api routing).
# Include the /api suffix — see src/services/api-client.ts
NEXT_PUBLIC_API_URL=https://your-domain.com/api
